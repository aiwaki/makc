interface:
  display_name: "Makc"
  short_description: "Repo-specific patterns and workflows for makc"
  default_prompt: "Use the makc repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true